1) git restore - откатывает изменения до коммита
2) git diff <path to file> - показывает изменения по сравнению с последним коммитом (если файлы добавлены в index)
   git diff --staged - показывает изменения по сравнению с посл-им коммитом (при нахождении файла в index)
