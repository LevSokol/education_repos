01) git config --global user.name "Qwerty Qwerty" - указание имени, от которого в репозиторий будут добавляться коммиты
02) git config --global user.email email@addres.domen - добавление почты
03) git init - создание репозитория
04) git restore - откатывает изменения до коммита
   git restore --staged - перемещение из index в unstaged
05) git diff <path to file> - показывает изменения по сравнению с последним коммитом (если файлы добавлены в index)
   git diff --staged - показывает изменения по сравнению с посл-им коммитом (при нахождении файла в index)
06) git commit -am (m = сообщение для коммита)
07) git log - p - вывод истории коммитов с более подробным описанием изменений
08) nano .gitignore - создание файла с исключениями для добавления изменений в index
09) git branch -v - просмотр существующих веток
10) git merge - слияние веток 
